# Project Rules for AI Assistant

- **NEVER Push to Remote Automatically**: Do NOT execute `git push` or any remote deployment commands unless the user explicitly requests a push or live deployment in their prompt.
- **Local Development Only**: All feature implementations and fixes must be kept local.
